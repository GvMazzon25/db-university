1)

SELECT COUNT(id), enrolment_date
FROM `students`
GROUP BY enrolment_date;


2)

SELECT COUNT(id), office_address
FROM `teachers`
GROUP BY office_address;


3)

SELECT COUNT(vote) 
FROM `exam_student` 
GROUP BY exam_id;


4)

SELECT COUNT(id)
FROM `departments` 
GROUP BY head_of_department;